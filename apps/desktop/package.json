{
  "name": "@PassShield/desktop",
  "version": "1.0.9",
  "private": true,
  "description": "PassShield — a local-first desktop password manager.",
  "main": "dist/main/main.js",
  "scripts": {
    "build:main": "tsc --build",
    "build:renderer": "npm run build --workspace @PassShield/renderer",
    "build": "npm run build:renderer && npm run build:main",
    "clean": "tsc --build --clean",
    "dev": "npm run build && electron .",
    "start": "electron .",
    "kill:app": "powershell -NoProfile -Command \"Get-Process -Name PassShield -ErrorAction SilentlyContinue | Stop-Process -Force; exit 0\"",
    "predist:win": "npm run kill:app",
    "dist:win": "npm run build && electron-builder --win",
    "predist:win:dir": "npm run kill:app",
    "dist:win:dir": "npm run build && electron-builder --win --dir",
    "predist:appx": "npm run kill:app",
    "dist:appx": "npm run build && electron-builder --win appx"
  },
  "dependencies": {
    "@PassShield/contracts": "*",
    "@PassShield/crypto": "*",
    "@PassShield/database": "*",
    "@PassShield/generator": "*",
    "@PassShield/validation": "*"
  },
  "devDependencies": {
    "electron": "44.1.0",
    "electron-builder": "^26.15.3"
  }
}
